/*
  Homepage.jsx
  - Page d'accueil de l'application. Actuellement c'est un squelette.
  - On importe des composants réutilisables (Button, Input) pour les utiliser
    ici si nécessaire.
  - Pour un projet réel, on ajouterait du contenu (liste d'articles récents,
    call-to-action, etc.)
*/

import Button from "../../components/Common/Button";
import Input from "../../components/Common/Input";

export default function Homepage() {
  // Page d'accueil vide pour l'instant — à personnaliser selon les besoins
  return <></>;
}
